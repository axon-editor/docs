---
title: Settings
description: Configure Axon behavior.
icon: Settings
---

import { Screenshot } from '@/components/docs/shared';

Axon stores user configuration in Electron's platform-specific application-data directory as `settings.json`. Use **Open Settings File** from Settings when you need the exact path. A project can also provide `axon.json` at its workspace root for project-level language and runtime choices.

<Screenshot
  src="/media/screenshots/axon-latest-04.png"
  alt="Axon settings modal"
  caption="The Settings UI groups appearance, editor, terminal, Git, and extension options into focused sections."
/>

## Opening Settings

- Click the Settings gear icon in the sidebar.
- Run `Open Settings` from the command palette.
- Use `Cmd+,` on macOS or `Ctrl+,` on Windows/Linux.

## Editor

```json
{
  "editor": {
    "fontPreset": "axon-default",
    "fontFamily": ".AxonMono",
    "fontSize": 14,
    "lineHeight": 22,
    "fontWeight": 400,
    "fontLigatures": true,
    "minimapEnabled": false,
    "cursorBlinking": "expand"
  }
}
```

## Appearance

```json
{
  "editor": {
    "themeId": "ayu-dark",
    "fontPreset": "axon-default",
    "uiFontFamily": ".AxonSans",
    "sidebarSide": "left"
  }
}
```

## Language Tools

```json
{
  "lsp": {
    "enabled": true,
    "pythonVirtualEnvPath": "",
    "pythonInterpreterPath": ""
  }
}
```

## Local Agent

```json
{
  "ai": {
    "enabled": true,
    "provider": "local",
    "model": "axon-code",
    "apiKeyEnv": "",
    "includeWorkspaceContext": true
  }
}
```

Axon normalizes settings against the supported schema. VS Code-style dotted keys such as `editor.fontSize`, `git.autofetch`, or `terminal.integrated.shell.osx` are not Axon settings and should not be documented as active options.
