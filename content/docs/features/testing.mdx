---
title: Test Explorer
description: Discover, run, and inspect project tests inside Axon.
icon: FlaskConical
---

import { Screenshot } from '@/components/docs/shared';

Axon includes a native testing surface so test discovery and results remain part of the active project instead of becoming detached terminal output.

<Screenshot
  src="/media/screenshots/captures/axon-capture-06.png"
  alt="Axon Test Explorer showing npm scripts, Go packages, pass counters, and completed test output"
  caption="Test Explorer groups npm scripts and Go packages, tracks run status, and switches between selected-target and full-run output inside the workspace."
/>

## Current Capabilities

- Discover tests through registered test providers.
- Run every discovered test or an individual target.
- Stream test output while a run is active.
- Show completed results inside the editor.
- Keep test processes and results scoped to the current workspace.

<Screenshot
  src="/media/screenshots/captures/axon-capture-05.png"
  alt="Axon Test Explorer streaming output from an active test run"
  caption="Active runs keep their live output, elapsed state, and stop control inside Test Explorer."
/>

## Workbench Integration

Testing is registered as a built-in extension with explicit command and view activation events. Toolbar actions, command palette entries, and sidebar navigation therefore enter the same activation lifecycle before opening the Test Explorer.

<Screenshot
  src="/media/screenshots/captures/axon-capture-03.png"
  alt="Axon Test Explorer ready to discover and run workspace tests"
  caption="The idle Test Explorer remains a registered workspace surface before a provider starts a run."
/>

## Planned Depth

Inline failure feedback, direct navigation to failing locations, coverage visualization, and test debugging are still being developed. These are not shipped capabilities yet.
