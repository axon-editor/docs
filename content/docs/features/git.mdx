---
title: Git Integration
description: Git status, diffs, history, and agent-assisted commits.
icon: GitBranch
---

import { Screenshot } from '@/components/docs/shared';

Axon includes built-in Git support for version control workflows. You can view changes, inspect diffs, browse a commit graph, manage branches, stashes, worktrees, conflicts, and draft commit messages without leaving the editor.

<Screenshot
  src="/media/screenshots/captures/axon-capture-30.png"
  alt="Axon Git Graph with selected commit details and repository history"
  caption="A selected commit opens scrollable details beside the searchable repository history and graph lanes."
/>

## File Tree Status

- Green: modified files.
- Red: deleted files.
- Blue: untracked files.
- Gray: ignored files.

## Gutter Markers

- Green bar: added line.
- Red bar: removed line.
- Blue bar: modified line.

## Source Control Panel

- View all changed files.
- Stage and unstage changes.
- View inline diffs.
- Write commit messages and commit.
- Push and pull from remotes.
- Inspect branch information.
- Open the Git graph as a normal editor tab.

<Screenshot
  src="/media/screenshots/captures/axon-capture-62.png"
  alt="Axon Source Control showing a modified file and its full diff"
  caption="Source Control keeps the changed-file list, staging controls, commit field, and selected file diff in one surface."
/>

## Commit Graph

- Search commits by subject, author, hash, branch, or tag.
- Inspect graph lanes, references, dates, authors, and short hashes in a stable table.
- Rest the pointer or keyboard focus on a commit for two seconds to see a preview without selecting it.
- Select a commit to open its independently scrollable details pane.
- Browse changed files in their real, collapsible repository folder structure.
- Select a changed file to open its commit-specific historical comparison view.

<Screenshot
  src="/media/screenshots/captures/axon-capture-29.png"
  alt="Axon Git Graph showing searchable commit history and graph lanes"
  caption="The graph overview keeps commit subjects, authors, dates, references, hashes, and graph lanes aligned in a stable table."
/>

<Screenshot
  src="/media/screenshots/captures/axon-capture-23.png"
  alt="Axon Git history showing a historical CSS diff"
  caption="Selecting a changed file opens its commit-specific comparison without losing the surrounding repository history."
/>

## Agent-Assisted Commits

Axon Agent can draft commit messages from the current Git diff. The terminal command `axon commit` reads staged changes, streams a message draft, and asks before running `git commit`.

<Screenshot
  src="/media/screenshots/captures/axon-capture-22.png"
  alt="Axon Git history sidebar listing recent commits"
  caption="Recent history remains available from the sidebar for quick commit inspection before a deeper graph review."
/>

## Still Improving

Active Git work includes richer conflict resolution, safer stash flows, cherry-pick and rebase flows, fuller worktree management, and later pull-request review integration.
