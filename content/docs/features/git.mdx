---
title: Git Integration
description: Git status, diffs, history, and agent-assisted commits.
icon: GitBranch
---

import { Screenshot } from '@/components/docs/shared';

Axon includes built-in Git support for version control workflows. You can view changes, inspect diffs, browse history, work with graph context, manage branches, stashes, worktrees, conflicts, and draft commit messages without leaving the editor.

<Screenshot
  src="/media/screenshots/axon-latest-02.png"
  alt="Axon Git commit detail showing metadata, changed files, and a side-by-side diff"
  caption="Commit detail keeps metadata and changed files beside a full side-by-side diff so each revision can be reviewed in context."
/>

## File Tree Status

- Green: modified files.
- Red: deleted files.
- Blue: untracked files.
- Gray: ignored files.

## Gutter Markers

- Green bar: added line.
- Red bar: removed line.
- Blue bar: modified line.

## Source Control Panel

- View all changed files.
- Stage and unstage changes.
- View inline diffs.
- Write commit messages and commit.
- Push and pull from remotes.
- Inspect branch information.
- Use graph, stash, worktree, and conflict surfaces as they mature.

<Screenshot
  src="/media/screenshots/axon-latest-03.png"
  alt="Axon Git history beside an editor completion list"
  caption="Git history remains available beside the editor, allowing code navigation and language features to continue while revisions are inspected."
/>

## Agent-Assisted Commits

Axon Agent can draft commit messages from the current Git diff. The terminal command `axon commit` reads staged changes, streams a message draft, and asks before running `git commit`.

## Still Improving

Active Git work includes richer conflict resolution, safer stash flows, cherry-pick and rebase flows, fuller worktree management, and later pull-request review integration.
